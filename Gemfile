source "https://rubygems.org"

# Matches the GitHub Pages build environment, including the
# `jekyll-remote-theme` / `jekyll-readme-index` plugins declared in
# `_config.yml`.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ dropped these from the standard library; needed for `jekyll serve`.
gem "webrick"
